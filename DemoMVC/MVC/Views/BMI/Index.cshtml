@model BMIModel

@{
    ViewBag.Title = "Tính chỉ số BMI";
}

<h2>Tính chỉ số BMI</h2>
<form asp-action="Index">
    <input type="text" name="Cân Nặng">
    <input type="text" name="Chiều Cao">
    <button type="submit">Tính BMI</button>
</form>